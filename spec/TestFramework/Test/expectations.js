export const expectToEqual = (actualValue, expectedValue) => {
  return actualValue === expectedValue;
}

export const expectToDeeplyEqual = (actualValue, expectedValue) => {
  return actualValue == expectedValue;
}
